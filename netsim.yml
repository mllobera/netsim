name: netsim

channels:
  - conda-forge
  
dependencies:
  - python >= 3.7
  - numpy >= 1.16.3
  - geopandas >= 0.5.0
  - cython >= 0.29.7
  - matplotlib
  - pandas
  - jupyter
  - jupyterlab
  - rasterio
  - networkx